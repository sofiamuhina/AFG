{
  "name": "AFG",
  "version": "0.1.0",
  "private": true,
  "description": "Проект AFG",
  "devDependencies": {
    "autoprefixer": "^7.1.2",
    "editorconfig-cli": "^0.2.1",
    "grunt": "1.0.1",
    "grunt-browser-sync": "^2.2.0",
    "grunt-contrib-watch": "^1.0.0",
    "grunt-postcss": "^0.8.0",
    "grunt-sass": "2.0.0",
    "load-grunt-tasks": "^3.5.2"
  },
"scripts": {
    "test": "editorconfig-cli",
    "build": "grunt sass && grunt postcss",
    "start": "npm run build && grunt serve"
  },
  "editorconfig-cli": [
    "*.html",
    "*.json",
    "*.js",
    "js/**/*.js",
    "img/**/*.svg",
    "sass/**/*.{sass,scss}"
  ],
  "engines": {
    "node": "6.10"
  }
}
